{
  "name": "fast-cart",
  "version": "1.0.0",
  "description": "an ecommerce back-end exercize, to demonstrate defining models and associations using the sequelize package, and perform RESTful CRUD operations.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "watch": "nodemon server.js",
    "seed": "node seeds/index.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/blchase215/fast-cart"
  },
  "keywords": ["ecommerce, api, RESTful CRUD, mySQL, sequelize"],
  "author": "Brian Chase",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.3"
  },
  "dependencies": {
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mysql2": "^2.1.0",
    "sequelize": "^5.21.7"
  }
}
